# 06. 태스크 분해 — 개발 실행 계획

> 표기: `[모듈] ID. 태스크 — 완료 기준` / 의존성은 `←`
> 순서 원칙(전략 §9): **A(소상공인 SME v1) 먼저 → B(크리에이터판 별도 레포는 미래)**. A는 워크스페이스 전용 패키지를 만들고 내부 소비한다. GitHub Packages 발행은 dist build와 release workflow가 준비된 뒤 유예 착수한다.

## Phase P — 유예된 사업·법무 준비 (Creator/유료 구독 착수 전)

> 아래 항목은 현재 SME v1 구현 블로커가 아니다. 토스 빌링, 카카오 알림톡/SMS, 환불정책은 미래 Creator 또는 유료 구독 제품 범위에서 별도 결정 후 진행한다.

- P-1. 사업자등록 확인 + 통신판매업 신고 필요성 재검토 (유료 구독 판매 시) ← 미래 유료화 결정 **(사용자)**
- P-2. 토스페이먼츠 가맹 계약 + 빌링(정기결제) 심사 — 미래 Creator/유료 구독 선행, 현재 SME v1 제외 ← P-1 **(사용자)**
- P-3. 약관·개인정보처리방침·환불정책 초안 — 유료 결제 도입 시 제품별로 분리 ← P-1
- P-4. 카카오 비즈메시지 발신프로필/SMS 폴백 — 미래 유료 알림 채널, 현재 SME v1 제외 ← P-1 **(사용자)**

## Phase A — boina 레이더 모듈 + UX 재구성 (소상공인) · 예상 4~5주 (07 재설계 반영 상향)
> 2026-07-07 우선순위 변경: 소상공인 SME v1의 남은 production smoke/파일럿 검증은 **보류**한다. 현재 실행 우선순위는 같은 엔진 기반의 Creator 제품이며, 공통 버그 수정은 엔진/공유 패키지에서 처리한다.


### A0. 거버넌스 (boina 규칙 준수)
- A0-1. **04+07 문서** 기반 boina `docs/planning/` Gap Review 패치 생성 (07 §5 패치 항목 6개: 홈 피드 전환·S3+S4 통합·라우트 재편 포함) → 게이트 판정 ← 없음
- A0-2. `radar_subscriptions` 범위 확인 + DECISION_LOG 기록: 현재 SME v1에서는 유료 빌링이 아니라 무료 `trialing/active` 주간 스캔 예약 리소스임을 명시 ← A0-1
- A0-3. **UX 재구성 실행** — 라우트 재편(home/status/rivals/write)·홈 피드 조립·S3+S4 통합. 로직·데이터 계층 재사용, **비주얼은 08 디자인 시스템("가게 성적표")으로 전면 교체** (토큰·M-01~M-07 컴포넌트·증거 시트·변화 카드) ← A0-1

### A1. 공유 패키지 (이후 모든 것의 기반)
- A1-1. `packages/keyword-pipeline` 신설 — 검증 폴더 JS→TS 이식 (expand/시드분해/collectSignals/naverScore), 유닛테스트(니치 시드 폴백 케이스 필수) ← A0-1
- A1-2. 네이버 API 키 4종 발급 + 완전 검증 실행 (검증 폴더 체크리스트 잔여 항목) ← 없음 **(사용자 작업 선행)**
- A1-3. 쿼터 가드 모듈 (일 예산 배정·소진 시 부분 결과) ← A1-1

### A2. 데이터·잡
- A2-1. Drizzle 스키마 4테이블 (radar_subscriptions/scans/keywords/feedback) + 마이그레이션 ← A0-1
- A2-2. 주간 스캔 잡 — 가게 fan-out, 실패 격리, 재시도 1회 ← A1-1, A2-1
- A2-3. 가게명 AI 프로브 연결 (기존 geo-validator 호출) ← A2-2

### A3. 화면·스캔 예약 (현재 SME v1, 결제/문자 알림 제외)
- A3-1. 홈 카드 ④ 미활성 상태 (실측 미리보기 1건 + 블러 티저 + 스캔 예약 CTA + 예시 폴백 라벨) ← A1-1
- A3-2. 홈 카드 ④ `trialing/active` 상태 (검색어 5개 + 글감 연결 + 빈/실패 상태 + 앱 내 딥링크) ← A2-2
- A3-3. 글감 연결 — 기존 snippets 모듈에 검색어 파라미터 전달 ← A3-2
- A3-4. `radar_subscriptions` 무료 상태 전이 구현 (`trialing`/`active`/비활성, 결제·토스·환불 없음) ← A0-2, A2-1
- A3-5. 알림은 현재 앱 내 상태/홈 카드 갱신으로 한정. 카카오 알림톡/SMS는 미래 유료 알림 범위로 유예 ← A2-2

### A4. 검증
- A4-1. E2E: 진단→홈 카드 ④ 미활성→스캔 예약 활성화(`trialing/active`)→월요 잡→홈 카드 ④ 활성→글감 복붙 전체 경로 ← A3.*
- A4-2. 실가게 5곳 파일럿 2주 (열람률·"썼어요" 응답 수집) ← A4-1

## Phase B — 크리에이터판 새 레포 · 예상 5~6주 — **현재 우선순위**

### B0. 기반
- B0-1. **완료(2026-07-07)** Creator 선행조건: `@boina/contracts`, `@boina/engine`, `@radar/keyword-pipeline` dist build/package exports/버전 태그 기반 GitHub Packages release workflow를 추가했다. 릴리스는 `packages-vX.Y.Z` 태그 또는 workflow_dispatch version으로 실행한다.
- B0-2. 레포 부트스트랩 (Next+Tailwind+Drizzle+Supabase+CI) ← 없음
- B0-3. 03 디자인 토큰 → Tailwind 설정 + 기본 컴포넌트 셸 (C-01~C-07 스토리북) ← B0-2

### B1. 레이더 코어
- B1-1. DB 스키마 전체 (05 §3) ← B0-2
- B1-2. 이중 점수 산식 `lib/scoring` — 순수 함수 + 유닛테스트 (근거 jsonb 왕복 포함) ← B1-1
- B1-3. 일일 스캔 잡 — 2단 제공(네이버 선노출·AI 후행) + SSE 스트리밍 ← B1-2, B0-1
- B1-4. S1 온보딩 (실시간 미리보기 debounce, 니치 폴백 안내) ← B0-3, B1-3
- B1-5. S2 대시보드 — 카드 그리드·필터·티커·4상태 + 성도 CSS 목업 fallback 유지 ← B1-4
- B1-5a. 성도 스파이크(반나절) — Pixi 글로우 룩·모바일 60fps·라벨 겹침·renderer.extract가 목업 CSS 대비 우위인지 실물 비교 후 채택 확정 (미달 시 CSS 버전 유지) ← B0-3, B1-5
- B1-5b. **성도 뷰(C-08) 제품 구현** — 채택된 경우에만 Pixi v8+pixi-filters(Glow/Bloom), d3-force 프리컴퓨트 배치(forceRadial=점수·forceCollide=라벨), 스윕·블립·성좌선, 60fps 검증, renderer.extract 공유 프레임 구현 ← B1-5a
- B1-6. S3 상세 패널 — 근거 블록·판정 라벨·브리핑 복사 ← B1-5
- B1-7. S2.5 즉시 조회 — ⌘K 오버레이, 단건 신호 수집 API(/api/lookups), 2단 제공(N 즉시·AI 옵트인), 한도 집행 ← B1-2, B0-3

### B2. 진단·추적
- B2-1. S4 글 진단 — runDiagnosisPipeline 연동, 체크리스트 매핑, 60s 단계 표시 ← B0-1, B1-1
- B2-2. 인용 프로브 오케스트레이션 `lib/probe` — 질문 세트 생성·버전 기록·판정 ← B2-1
- B2-3. S5 인용 추적 — 타임라인·빈 상태 코칭·방법론 각주 ← B2-2
- B2-4. 주간 프로브 잡 + 원가 상한 가드 ← B2-2

### B3. 수익·마감
- B3-1. 토스 빌링 — 미래 Creator 유료 구독 범위: 빌링키 발급·해지·웹훅 서명 검증·멱등 처리·3일 실패 유예·무료 강등·환불정책 문구 일치 ← B0-2, P-3
- B3-2. 플랜 한도 집행 (usage 테이블·402 처리·업그레이드 유도) ← B3-1
- B3-3. S6 주간 리포트 + 메일 발송 ← B1-6, B2-3
- B3-4. S7 설정 (해지 1클릭·결제실패 배너) ← B3-1
- B3-5. 랜딩 페이지 (다크 관제실 — S2 실화면 캡처 중심) ← B1-5

### B4. 품질 게이트
- B4-0. **디바이스 QA 패스 (양방향)** — 폰: 390px 전 화면 완결성·탭 타깃 44px·바텀시트·중급기 성도 60fps / PC: 1280·1440·1920 레이아웃(여백이 아닌 정보 확대 확인)·키보드 단축키 전체(⌘K·방향키·s/d·Ctrl+숫자)·hover 강화 동작 ← B3.* **(B4-1~3보다 먼저)**
- B4-1. 접근성 패스 (대비·키보드 순회·reduced-motion) ← B3.*
- B4-2. 성능 (모바일 LCP<3s·데스크톱<2.5s, 카드 가상화, 저사양 Bloom 오프 동작) ← B3.*
- B4-3. 보안 체크리스트 (05 §6) 전 항목 증거 확보 ← B3.*
- B4-4. 베타 오픈 (스레드 청중) ← B4-1~3

## 크리티컬 패스

```
현재 우선순위(2026-07-07):
B0-1(완료: packages dist/export/release workflow)
  → B0-2(creator 레포 부트스트랩: Next+Tailwind+Drizzle+Supabase+CI)
  → B0-3(디자인 토큰 + C-01~C-07 컴포넌트 셸)
  → B1-1(DB 스키마) → B1-2(이중 점수 산식)
  → B1-3(일일 스캔 잡: 네이버 선노출·AI 후행 + SSE)
  → B1-4(S1 온보딩) → B1-5(S2 대시보드)
  → B2-1(S4 글 진단: runDiagnosisPipeline 연동)
  → B4-4(베타)

보류:
SME A4 production smoke/AC-7/파일럿은 현재 Creator 우선순위에서 제외한다.
P-2/P-3/P-4(토스·정책·카카오/SMS)는 미래 유료 구독/Creator 수익화 착수 전 선행이며 B0~B2 엔진/제품 검증의 블로커가 아니다.
```

**지금 당장 (개발 세션 첫 30분)**: ① B0-1 변경 커밋/푸시 ② B0-2 Creator 레포 부트스트랩 ③ B0-3 디자인 토큰/컴포넌트 셸 착수 ④ B1-1 Creator DB 스키마 설계
